# HW 1
- [src/regex_hw.py](src/regex_hw.py) contains code to generate final deliverable. The final output is called `OH_results.csv`
- Manually labeled dataset is saved in `dataset_OH_label.csv` and submitted.